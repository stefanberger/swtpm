subdir('utils')

subdir('swtpm')
subdir('swtpm_bios')
if with_gnutls
  subdir('swtpm_cert')
endif
subdir('swtpm_ioctl')
subdir('swtpm_localca')
subdir('swtpm_setup')

if with_selinux
  subdir('selinux')
endif
